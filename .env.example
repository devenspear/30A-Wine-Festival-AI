# Required - AI Model (OpenAI GPT-4o Mini)
OPENAI_API_KEY=sk-proj-...

# Required for full features - Vector Database (Upstash Vector)
# Without this, the searchGeneral tool returns fallback info instead of vector search
UPSTASH_VECTOR_REST_URL=https://...
UPSTASH_VECTOR_REST_TOKEN=...

# Required for full features - Rate Limiting & Analytics (Upstash Redis)
# Without this, rate limiting and analytics are disabled (fail-open)
UPSTASH_REDIS_REST_URL=https://...
UPSTASH_REDIS_REST_TOKEN=...

# Optional - Analytics (Vercel KV - alternative to Upstash Redis)
KV_REST_API_URL=https://...
KV_REST_API_TOKEN=...

# Optional - Admin Dashboard (/api/admin/analytics?auth=Bearer YOUR_PASSWORD)
ADMIN_PASSWORD=your_secure_password

# Optional - Cron Jobs (Vercel cron cleanup of old analytics)
CRON_SECRET=your_cron_secret

# Auto-set by Vercel (do not set manually)
# NEXT_PUBLIC_VERCEL_GIT_COMMIT_SHA=
# VERCEL_URL=

# NOT required - Weather uses Open-Meteo API (free, no key needed)
